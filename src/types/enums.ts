export enum Gender {
  Male = 'male',
  Female = 'female',
  Other = 'other',
}

export enum Currency {
  Dollar = 'USD',
  Nis = 'ILS',
  Euro = 'EUR',
}

export enum TransactionSide {
  Buyer = 'buyer',
  Seller = 'seller',
  Admin = 'admin',
}
